# 文件上传监控 (File Upload Monitor)

一个用于Windows平台的Flutter桌面应用程序，可以监控文件夹并自动上传新文件到指定接口。

A Flutter desktop application for Windows that monitors folders and automatically uploads new files to a specified API endpoint.

## 功能特性 (Features)

- ✅ **自动监控文件夹** - 实时监控指定文件夹的新文件
- ✅ **自动上传** - 新文件自动上传到配置的API接口
- ✅ **文件管理** - 上传成功后自动移动到uploaded文件夹
- ✅ **手动上传** - 支持手动选择文件上传
- ✅ **重试机制** - 上传失败自动重试3次
- ✅ **上传日志** - 详细记录上传历史，支持筛选
- ✅ **现代UI** - 科技感深色主题，渐变色设计
- ✅ **数据持久化** - 设置和日志自动保存

## 快速开始 (Quick Start)

### 运行开发版本

```bash
flutter run -d windows
```

### 构建Windows可执行文件

```bash
flutter build windows --release
```

可执行文件位置: `build/windows/runner/Release/watch_file_uploader.exe`

## 使用说明 (Usage)

1. **配置监控文件夹**
    - 点击"选择文件夹"按钮
    - 选择要监控的文件夹

2. **配置上传地址**
    - 输入API上传地址 (例如: `https://your-api.com/upload`)
    - 点击保存图标

3. **开始监控**
    - 点击"开始监控"按钮
    - 新文件会自动上传并移动到uploaded文件夹

4. **手动上传**
    - 点击"手动上传"按钮
    - 选择要上传的文件

## 技术栈 (Tech Stack)

- **Flutter** - 跨平台UI框架
- **Provider** - 状态管理
- **Dio** - HTTP客户端
- **Watcher** - 文件系统监控
- **SharedPreferences** - 数据持久化
- **Google Fonts** - 现代字体

## 项目结构 (Project Structure)

```
lib/
├── models/              # 数据模型
├── services/            # 业务服务
├── providers/           # 状态管理
├── screens/             # 页面
├── widgets/             # UI组件
└── theme/               # 主题配置
```

## 系统要求 (Requirements)

- Windows 10 或更高版本
- 无需额外运行时依赖

## 开发者 (Developer)

Built with Flutter 💙

## 许可证 (License)

MIT License
